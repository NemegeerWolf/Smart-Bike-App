<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TestBluethoot.MainPage">

    <StackLayout>
        <Button Text="click me" Clicked="Button_Clicked" />
        <ListView x:Name="pk" ItemSelected="pk_SelectedIndexChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"  Margin="20,10,20,0">
                            <Label Text="{Binding Name}" FontSize="18" TextColor="#273389"/>
                            <Label Text="{Binding Uuid}" FontSize="14" TextColor="#aaa"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <Picker x:Name="pkc" SelectedIndexChanged="pk_SelectedIndexChanged"/>
        <Label x:Name="lbl" Text="hi" BackgroundColor="Red" VerticalOptions="StartAndExpand"/>

    </StackLayout>

</ContentPage>
